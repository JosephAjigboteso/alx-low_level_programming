#include "main.h"
/**
 * int_strlem - returns length of string
 * @s: character to get string
 * Return: returns length
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{}
return (i);
}
